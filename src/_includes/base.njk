<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{{ description or 'Bürgerinitiative Zukunft Bernöwe - Engagierte Dorfbewohner setzen sich ehrenamtlich für die Zukunft von Bernöwe ein. Gemeinschaftliche Veranstaltungen, Projekte und Aktionen für ein lebenswertes Dorfleben.' }}">
    
    <!-- Favicon and icons -->
    <link rel="icon" type="image/x-icon" href="/assets/favicon.ico">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon-32x32.png">
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/apple-touch-icon.png">
    <link rel="manifest" href="/assets/site.webmanifest">
    <meta name="theme-color" content="#0284c7">
    
    <!-- Critical CSS inlined -->
    <style>
      /* CRITICAL CSS - Essential above-the-fold styles only */
      :root{--clr-primary-50:#f0f9ff;--clr-secondary-50:#f0fdf4;--clr-neutral-50:#f8fafc;--clr-neutral-800:#1e293b;--fs-400:1rem;--fs-800:3rem;--fw-700:700}*{box-sizing:border-box}html{scroll-behavior:smooth;scroll-padding-top:100px}body,h1,p{margin:0}body{min-height:100vh;font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;font-size:var(--fs-400);color:var(--clr-neutral-800);line-height:1.6}main{contain:layout style}h1{font-size:var(--fs-800);font-weight:var(--fw-700);contain:layout style}.page-header{background:var(--clr-neutral-50);position:sticky;top:0;z-index:50;min-height:100px;contain:layout style}.hero{background:linear-gradient(135deg,var(--clr-primary-50),var(--clr-secondary-50));contain:layout style}.hero>div{min-height:600px}@media (max-width:768px){html{scroll-padding-top:70px}.page-header{min-height:auto}.hero-image-container{height:424px}}
    </style>
    
    <!-- Preload LCP image for faster discovery -->
    <link rel="preload" href="/assets/images/b0m5Bm8HQ4-369.avif" as="image" fetchpriority="high">
    
    <!-- Load full CSS asynchronously with fallback -->
    <link rel="preload" href="{{ './../../style.css' | asset }}" as="style" onload="this.onload=null;this.rel='stylesheet'">
    <noscript><link rel="stylesheet" href="{{ './../../style.css' | asset }}"></noscript>
    
    <title>{{ title }}</title>
  </head>
  <body>
    {% include 'header.njk' %}
    <main>
      {{ content | safe }}
    </main>
    {% include 'footer.njk' %}

    <script>
      // Simple bot protection for contact info
      function initContacts() {
        const emailEl = document.querySelector('[data-email]');
        if (emailEl) {
          const email = atob(emailEl.getAttribute('data-email')).split('').reverse().join('');
          emailEl.textContent = email;
          emailEl.parentElement.href = 'mailto:' + email;
          emailEl.parentElement.onclick = null;
        }
        
        const phoneEl = document.querySelector('[data-phone]');
        if (phoneEl) {
          const phone = atob(phoneEl.getAttribute('data-phone')).split('').reverse().join('');
          phoneEl.textContent = phone;
          phoneEl.parentElement.href = 'tel:' + phone;
          phoneEl.parentElement.onclick = null;
        }
      }
      
      window.addEventListener('load', initContacts);
    </script>
  </body>
</html>